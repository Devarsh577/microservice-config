spring:
  datasource:
    url: jdbc:mysql://localhost:3306/categorydb
    username: user
    password: user123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
config:
   value: "Category-service , We are in Development. rebbit mq is working fine. UPDATED!!"